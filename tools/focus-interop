#!/usr/bin/env node

import('./focus-interop-cli.mjs').then(({ runCli }) => runCli(process.argv.slice(2))).catch((error) => {
  console.error(error?.message || String(error));
  process.exitCode = 1;
});
